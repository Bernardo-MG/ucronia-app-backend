openapi: 3.0.3
info:
  title: Ucronia API
  version: 1.0.0
  description: API for managing the Ucronia association.
servers:
  - url: https://localhost
    description: Local development
paths:
  /transaction/type:
    $ref: paths/transaction_types.yml
  /transaction/type/{number}:
    $ref: paths/transaction_types_by_id.yml