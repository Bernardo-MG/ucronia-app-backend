components:
  schemas:
    TransactionMonthlyEvolution:
      type: object
      required: [results, total]
      properties:
        month:
          $ref: '../common/dates.yml#/components/schemas/Date'
        results:
          type: number
          format: float
        total:
          type: number
          format: float
    TransactionMonthlyEvolutionResponse:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/TransactionMonthlyEvolution'