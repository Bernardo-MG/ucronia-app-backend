components:
  schemas:
    TransactionSummary:
      type: object
      required: [results, total]
      properties:
        results:
          type: number
          format: float
          minimum: 0
        total:
          type: number
          format: float
          minimum: 0
    TransactionSummaryResponse:
      type: object
      properties:
        content:
          $ref: '#/components/schemas/TransactionSummary'