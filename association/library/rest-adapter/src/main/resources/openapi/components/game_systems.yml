components:
  parameters:
    gameSystemsSortParam:
      name: sort
      description: Sorting to apply.
      in: query
      style: form
      explode: true
      schema:
        type: array
        items:
          type: string
          pattern: '^(name|number)\|(asc|desc)$'
          example: name|asc
  schemas:
    GameSystem:
      type: object
      properties:
        number:
          $ref: '../common/ids.yml#/components/schemas/Id'
        name:
          type: string
          minLength: 1
          maxLength: 100
          example: 'd20'
    GameSystemCreation:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
          example: 'd20'
    GameSystemUpdate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
          example: 'd20'
    GameSystemPageResponse:
      allOf:
        - $ref: '../common/responses.yml#/components/schemas/PageResponse'
        - type: object
          properties:
            content:
              type: array
              items:
                $ref: '#/components/schemas/GameSystem'
    GameSystemResponse:
      type: object
      properties:
        content:
          $ref: '#/components/schemas/GameSystem'