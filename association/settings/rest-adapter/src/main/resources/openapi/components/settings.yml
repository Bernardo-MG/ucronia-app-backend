components:
  parameters:
    settingsSortParam:
      name: sort
      description: Sorting to apply.
      in: query
      style: form
      explode: true
      schema:
        type: array
        items:
          type: string
          pattern: '^(code|type)\|(asc|desc)$'
          example: code|asc
  schemas:
    PublicSettings:
      type: object
      properties:
        map:
          type: string
          maxLength: 300
        calendar:
          type: string
          maxLength: 300
        email:
          type: string
          maxLength: 300
        instagram:
          type: string
          maxLength: 300
    PublicSettingsResponse:
      type: object
      properties:
        content:
          $ref: '#/components/schemas/PublicSettings'