parameters:
  - in: path
    name: folderNumber
    required: true
    schema:
       $ref: '../common/ids.yml#/components/schemas/Id'
  - in: path
    name: imageNumber
    required: true
    schema:
       $ref: '../common/ids.yml#/components/schemas/Id'
put:
  summary: Move an image into a folder.
  operationId: moveImageToFolder
  tags: [Image folder]
  responses:
    '200':
      description: Image moved.
      content:
        application/json:
          schema:
            $ref: '../components/images.yml#/components/schemas/ImageResponse'
