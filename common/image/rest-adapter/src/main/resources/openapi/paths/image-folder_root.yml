parameters:
  - in: path
    name: imageNumber
    required: true
    schema:
       $ref: '../common/ids.yml#/components/schemas/Id'
delete:
  summary: Move an image to the root.
  operationId: moveImageToRoot
  tags: [Image folder]
  responses:
    '200':
      description: Image moved to the root.
      content:
        application/json:
          schema:
            $ref: '../components/images.yml#/components/schemas/ImageResponse'
