post:
  summary: Create a transaction.
  operationId: createTransaction
  tags: [Transaction]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/transactions.yml#/components/schemas/TransactionCreation'
  responses:
    '201':
      description: Transaction created.
      content:
        application/json:
          schema:
            $ref: '../components/transactions.yml#/components/schemas/TransactionResponse'

get:
  summary: Get all transactions.
  operationId: getAllTransactions
  tags: [Transaction]
  parameters:
    - $ref: '../common/pagination.yml#/components/parameters/pageParam'
    - $ref: '../common/pagination.yml#/components/parameters/sizeParam'
    - $ref: '../components/transactions.yml#/components/parameters/transactionsSortParam'
    - name: date
      description: Date to query for.
      in: query
      schema:
        $ref: '../common/dates.yml#/components/schemas/Date'
    - name: from
      description: Starting date to query for.
      in: query
      schema:
        $ref: '../common/dates.yml#/components/schemas/Date'
    - name: to
      description: Ending date to query for.
      in: query
      schema:
        $ref: '../common/dates.yml#/components/schemas/Date'
  responses:
    '200':
      description: Transactions page
      content:
        application/json:
          schema:
            $ref: '../components/transactions.yml#/components/schemas/TransactionPageResponse'