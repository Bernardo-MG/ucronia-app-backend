components:
  schemas:
    TransactionCalendarMonthsRange:
      type: object
      required: [months]
      properties:
        months:
          type: array
          items:
            $ref: '../common/dates.yml#/components/schemas/Month'
    TransactionCalendarMonthsRangeResponse:
      type: object
      properties:
        content:
          $ref: '#/components/schemas/TransactionCalendarMonthsRange'