components:
  schemas:
    FeePage:
      allOf:
        - $ref: '../common/pagination.yml#/components/schemas/Page'
        - type: object
          properties:
            content:
              type: array
              items:
                $ref: '#/components/schemas/Fee'
    Fee:
      type: object
      required: [member, month, paid]
      properties:
        member:
          $ref: 'contacts.yml#/components/schemas/MinimalContact'
        month:
          $ref: '../common/dates.yml#/components/schemas/Month'
        paid:
          type: boolean
        transaction:
          $ref: 'transactions.yml#/components/schemas/MinimalTransaction'
    FeeCreation:
      type: object
      required: [member, month]
      properties:
        member:
          $ref: '../common/ids.yml#/components/schemas/Id'
        month:
          $ref: '../common/dates.yml#/components/schemas/Date'
    FeePayments:
      type: object
      required: [member, transaction, months]
      properties:
        member:
          $ref: '../common/ids.yml#/components/schemas/Id'
        transaction:
          $ref: '../common/dates.yml#/components/schemas/Date'
        months:
          type: array
          items:
            $ref: '../common/dates.yml#/components/schemas/Month'
    FeeChange:
      type: object
      required: [month, transaction]
      properties:
        month:
          $ref: '../common/dates.yml#/components/schemas/Month'
        transaction:
          $ref: 'transactions.yml#/components/schemas/MinimalTransaction'