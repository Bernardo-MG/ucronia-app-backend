components:
  schemas:
    MinimalTransaction:
      type: object
      required: [index, date]
      properties:
        index:
          $ref: '../common/ids.yml#/components/schemas/Id'
        date:
          $ref: '../common/dates.yml#/components/schemas/Date'