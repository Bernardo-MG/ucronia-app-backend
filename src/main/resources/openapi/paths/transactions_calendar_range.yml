get:
  summary: Get calendar range.
  operationId: getTransactionCalendarRange
  tags: [TransactionCalendar]
  responses:
    '200':
      description: Calendar range
      content:
        application/json:
          schema:
            $ref: '../components/transactions_calendar.yml#/components/schemas/TransactionCalendarMonthsRangeResponse'