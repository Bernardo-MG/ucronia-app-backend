get:
  summary: Get members available to assign.
  operationId: getAvailableContacts
  tags: [UserContact]
  parameters:
    - $ref: '../common/pagination.yml#/components/parameters/pageParam'
    - $ref: '../common/pagination.yml#/components/parameters/sizeParam'
    - $ref: '../common/pagination.yml#/components/parameters/sortParam'
  responses:
    '200':
      description: List of available contacts.
      content:
        application/json:
          schema:
            $ref: '../components/contacts.yml#/components/schemas/ContactPageResponse'
