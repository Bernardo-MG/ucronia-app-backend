get:
  summary: Get members available to assign.
  operationId: getAvailablePersons
  tags: [UserPerson]
  parameters:
    - $ref: '../common/pagination.yml#/components/parameters/pageParam'
    - $ref: '../common/pagination.yml#/components/parameters/sizeParam'
    - $ref: '../components/persons.yml#/components/parameters/sortParam'
  responses:
    '200':
      description: List of available persons.
      content:
        application/json:
          schema:
            $ref: '../components/persons.yml#/components/schemas/PersonPageResponse'
